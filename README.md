### CURRENTLY IN DEVELOPMENT

Scaffold a new service under _apps/<name>.
 
Usage: npm run make:service -- <name> [--port <number>]

 Example:

   `npm run make:service -- auth`

   `npm run make:service -- payments`

   `npm run make:service -- payments --port 5005`